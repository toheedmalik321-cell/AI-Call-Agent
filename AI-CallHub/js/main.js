document.addEventListener("DOMContentLoaded", () => {

    // ===============================
    // AOS Animation
    // ===============================
    AOS.init({
        duration: 700,
        once: true,
        offset: 60,
        easing: "ease-out-cubic"
    });

});